# Supabase/PostgreSQL connection string. Keep the real value in backend/.env.
DATABASE_URL=postgresql+psycopg://USER:PASSWORD@HOST:5432/postgres

# Comma-separated origins. For development you can use *.
# For production, use your actual Cloudflare frontend URLs instead.
CORS_ORIGINS=*
